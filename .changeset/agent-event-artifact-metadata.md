---
'@a2x/sdk': minor
---

Let text, file, and data AgentEvents set durable Artifact descriptors and per-delivery metadata on streamed artifact updates. The executor snapshots nested metadata at yield time so later agent mutations cannot alter emitted or persisted values.
