---
"@a2x/sdk": patch
---

Keep `A2XClient` x402 spending caps and offer identity authoritative when payment hooks retain, mutate, insert, or reorder payment requirements, while accepting unchanged structural copies returned by existing selectors.
