---
'@a2x/sdk': minor
---

Add opt-in conversation-scoped `upto` metering with CAS-backed turn leases, idle/deadline/budget settlement triggers, zero-usage lapse handling, and restart-safe reconciliation records.
