---
"@a2x/sdk": patch
---

Preserve requirement-specific OAuth and OpenID Connect scopes on normalized client schemes as `params.requiredScopes` so providers request only the privileges selected by the AgentCard requirement.
